GameListResponse:
  content:
    application/json:
      schema:
        examples:
          - $ref: '../../../game.json'
        type: object
        properties:
          data:
            array:
              items:
                $ref: '../schemas/models.yaml#/Game'


GameSavedResponse:
  description: Score recorded successfully
  content:
    application/json:
      schema:
        type: object
        properties:
          data:
            array:
              items:
                $ref: '../schemas/models.yaml#/GameSavedResponseObject'
