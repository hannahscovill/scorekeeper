GameListResponse:
  content:
    application/json:
      schema:
        $ref: '../schemas/models.yaml#/Game'
      examples:
        game_started:
          $ref: '../../../data-examples/game.json'
        game_won:
          $ref: '../../../data-examples/game-copy.json'


GameSavedResponse:
  description: Score recorded successfully
  content:
    application/json:
      schema:
        type: object
        properties:
          data:
            array:
              items:
                $ref: '../schemas/models.yaml#/GameSavedResponseObject'
